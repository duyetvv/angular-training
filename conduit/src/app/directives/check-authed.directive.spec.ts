import { CheckAuthedDirective } from './check-authed.directive';

describe('CheckAuthedDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckAuthedDirective();
    expect(directive).toBeTruthy();
  });
});
